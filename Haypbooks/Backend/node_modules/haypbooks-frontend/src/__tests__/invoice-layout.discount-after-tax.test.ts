// Replaced by .tsx variant of this test to allow JSX parsing under TypeScript.
// Keep a trivial placeholder so jest doesn't mark the suite as empty
describe('legacy placeholder - invoice-layout.discount-after-tax', () => {
	it('placeholder', () => { expect(true).toBe(true) })
})
