const fs=require('fs'); const path=require('path'); const ln=parseInt(process.argv[2],10); if(!ln){console.error('Usage: node dump_line_chars.js <lineNumber>'); process.exit(1);} const fp=path.resolve(__dirname,'..','prisma','schema.prisma'); const s=fs.readFileSync(fp,'utf8'); const lines=s.split(/\r?\n/); const line=lines[ln-1]; if(!line){console.error('No line',ln); process.exit(1);} console.log('Line',ln,':',line); for(let i=0;i<line.length;i++){ const ch=line[i]; const code=line.charCodeAt(i); process.stdout.write((i+1)+':'+(ch===' ' ? '[space]' : ch)+'('+code+') ');} console.log('');