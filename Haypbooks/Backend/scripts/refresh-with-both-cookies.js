const axios = require('axios');
(async () => {
  // Replace these values with output from previous run if needed, or run login first
  console.log('This helper expects to be called right after a login and to accept token+refreshToken values.');
})();