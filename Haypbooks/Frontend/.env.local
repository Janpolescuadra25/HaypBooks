# Backend API URL
# In local dev we route API calls through the frontend dev server so they are same-origin.
# Point this at the frontend dev server (http://localhost:3000) so client requests go through
# the Next.js proxy rewrites and reach backend routes like /auth/pin/verify correctly.
NEXT_PUBLIC_API_URL=http://localhost:3000
