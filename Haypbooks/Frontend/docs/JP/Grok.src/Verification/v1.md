this the form for selections after sign up in for otp verification for email and phone number


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm Your Identity - HaypBooks</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
  <style>
    body { font-family: 'Inter', sans-serif; }
    .option-card:hover { transform: translateY(-4px); box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04); }
  </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-teal-50 to-emerald-50 flex items-center justify-center px-4 py-12">
  <div class="bg-white rounded-3xl shadow-2xl max-w-lg w-full p-10 border border-white/20">
    <!-- Header -->
    <div class="text-center mb-10">
      <div class="w-20 h-20 bg-emerald-600 rounded-2xl flex items-center justify-center text-white text-4xl font-bold mx-auto mb-6 shadow-lg">
        HB
      </div>
      <h2 class="text-3xl font-bold text-slate-900">Confirm Your Identity</h2>
      <p class="mt-3 text-lg text-slate-600">
        Choose how you’d like to receive your verification code.
      </p>
    </div>

    <!-- Form -->
    <form id="mfaPreferenceForm" action="/api/setup-mfa-preference" method="POST" class="space-y-6">
      <!-- Email Option -->
      <label class="option-card flex items-center p-6 border-2 border-slate-200 rounded-2xl cursor-pointer hover:border-emerald-500 transition peer-checked:bg-emerald-50/50 peer-checked:border-emerald-500">
        <input type="radio" name="preferred_method" value="email" checked class="peer form-radio h-6 w-6 text-emerald-600 focus:ring-emerald-500">
        <div class="ml-5 flex-1">
          <div class="font-bold text-xl text-slate-900">Email</div>
          <div class="text-slate-600 mt-1">
            Verification code will be sent to:<br>
            <span class="font-semibold">y***e@gmail.com</span>
          </div>
        </div>
      </label>

      <!-- SMS Option -->
      <label class="option-card flex items-center p-6 border-2 border-slate-200 rounded-2xl cursor-pointer hover:border-emerald-500 transition peer-checked:bg-emerald-50/50 peer-checked:border-emerald-500">
        <input type="radio" name="preferred_method" value="phone" class="peer form-radio h-6 w-6 text-emerald-600 focus:ring-emerald-500">
        <div class="ml-5 flex-1">
          <div class="font-bold text-xl text-slate-900">Text Message (SMS)</div>
          <div class="text-slate-600 mt-1">
            Verification code will be sent to:<br>
            <span class="font-semibold">+63 ••• ••• 6789</span>
          </div>
        </div>
      </label>

      <!-- Continue Button -->
      <button type="submit"
              class="mt-8 w-full bg-gradient-to-r from-emerald-600 to-teal-600 text-white py-4 rounded-2xl font-bold text-lg hover:from-emerald-700 hover:to-teal-700 transition shadow-xl hover:shadow-2xl">
        Continue
      </button>
    </form>

    <!-- Footer Note -->
    <p class="text-center text-sm text-slate-600 mt-8">
      You can change your preferred method anytime in <span class="font-semibold text-emerald-600">Password & Security</span> settings.
    </p>
  </div>
</body>
</html>

============================================================================================

for email if the user choose email it will direct to this form below :

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm Your Identity - HaypBooks</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
  <style>
    body { font-family: 'Inter', sans-serif; }
    .otp-input {
      width: 56px;
      height: 56px;
      text-align: center;
      font-size: 1.8rem;
      font-weight: bold;
    }
    @media (min-width: 640px) {
      .otp-input {
        width: 64px;
        height: 64px;
        font-size: 2rem;
      }
    }
    .otp-input:focus {
      outline: none;
      border-color: #10b981;
      box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.2);
    }
  </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-teal-50 to-emerald-50 flex items-center justify-center px-4 py-12">
  <div class="bg-white rounded-3xl shadow-2xl max-w-lg w-full p-10 border border-white/20">
    <!-- Header -->
    <div class="text-center mb-10">
      <div class="w-20 h-20 bg-emerald-600 rounded-2xl flex items-center justify-center text-white text-4xl font-bold mx-auto mb-6 shadow-lg">
        HB
      </div>
      <h2 class="text-3xl font-bold text-slate-900">Confirm Your Identity</h2>
      <p class="mt-3 text-lg text-slate-600">
        We sent a 6-digit code to your email
      </p>
    </div>

    <!-- OTP Form -->
    <form class="space-y-8">
      <!-- Six OTP Boxes - Always in One Row -->
      <div class="flex justify-center gap-3 sm:gap-4">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp1" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp2" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp3" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp4" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp5" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp6" autocomplete="off">
      </div>

      <!-- Remember Me Checkbox -->
      <div class="flex items-center justify-center space-x-3">
        <input type="checkbox" id="remember" class="w-5 h-5 text-emerald-600 rounded focus:ring-emerald-500">
        <label for="remember" class="text-slate-700 font-medium cursor-pointer">
          Remember me for 30 days on this device
        </label>
      </div>

      <!-- Buttons -->
      <div class="flex flex-col sm:flex-row gap-4">
        <button type="button" class="flex-1 border border-slate-300 text-slate-700 py-3 rounded-2xl font-semibold hover:bg-slate-50 transition">
          Resend Code
        </button>
        <button type="submit" class="flex-1 bg-gradient-to-r from-emerald-600 to-teal-600 text-white py-3 rounded-2xl font-semibold hover:from-emerald-700 hover:to-teal-700 transition shadow-xl hover:shadow-2xl">
          Verify
        </button>
      </div>
    </form>

    <!-- Footer Link -->
    <p class="text-center mt-8">
      <a href="#" class="text-emerald-600 hover:text-emerald-700 font-medium">
        Try a different verification method
      </a>
    </p>
  </div>

  <script>
    // Auto-focus next input on typing
    const inputs = document.querySelectorAll('.otp-input');
    inputs.forEach((input, index) => {
      input.addEventListener('input', (e) => {
        const value = e.target.value;
        if (/^\d$/.test(value)) {
          if (index < inputs.length - 1) {
            inputs[index + 1].focus();
          }
        } else {
          e.target.value = '';
        }
      });

      input.addEventListener('keydown', (e) => {
        if (e.key === 'Backspace' && !e.target.value && index > 0) {
          inputs[index - 1].focus();
        }
      });
    });

    // Focus first input on load
    inputs[0].focus();
  </script>
</body>
</html>


================================================================================

if the user choose phone it will direct to this form below :

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm Your Identity - HaypBooks</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
  <style>
    body { font-family: 'Inter', sans-serif; }
    .otp-input {
      width: 56px;
      height: 56px;
      text-align: center;
      font-size: 1.8rem;
      font-weight: bold;
    }
    @media (min-width: 640px) {
      .otp-input {
        width: 64px;
        height: 64px;
        font-size: 2rem;
      }
    }
    .otp-input:focus {
      outline: none;
      border-color: #10b981;
      box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.2);
    }
  </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-teal-50 to-emerald-50 flex items-center justify-center px-4 py-12">
  <div class="bg-white rounded-3xl shadow-2xl max-w-lg w-full p-10 border border-white/20">
    <!-- Header -->
    <div class="text-center mb-10">
      <div class="w-20 h-20 bg-emerald-600 rounded-2xl flex items-center justify-center text-white text-4xl font-bold mx-auto mb-6 shadow-lg">
        HB
      </div>
      <h2 class="text-3xl font-bold text-slate-900">Confirm Your Identity</h2>
      <p class="mt-3 text-lg text-slate-600">
        We sent a 6-digit code to your phone via SMS
      </p>
    </div>

    <!-- OTP Form -->
    <form class="space-y-8">
      <!-- Six OTP Boxes - Always in One Row -->
      <div class="flex justify-center gap-3 sm:gap-4">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp1" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp2" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp3" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp4" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp5" autocomplete="off">
        <input type="text" maxlength="1" inputmode="numeric" pattern="[0-9]*" class="otp-input border-2 border-slate-300 rounded-xl focus:border-emerald-500" id="otp6" autocomplete="off">
      </div>

      <!-- Remember Me Checkbox -->
      <div class="flex items-center justify-center space-x-3">
        <input type="checkbox" id="remember" class="w-5 h-5 text-emerald-600 rounded focus:ring-emerald-500">
        <label for="remember" class="text-slate-700 font-medium cursor-pointer">
          Remember me for 30 days on this device
        </label>
      </div>

      <!-- Buttons -->
      <div class="flex flex-col sm:flex-row gap-4">
        <button type="button" class="flex-1 border border-slate-300 text-slate-700 py-3 rounded-2xl font-semibold hover:bg-slate-50 transition">
          Resend Code
        </button>
        <button type="submit" class="flex-1 bg-gradient-to-r from-emerald-600 to-teal-600 text-white py-3 rounded-2xl font-semibold hover:from-emerald-700 hover:to-teal-700 transition shadow-xl hover:shadow-2xl">
          Verify
        </button>
      </div>
    </form>

    <!-- Footer Link -->
    <p class="text-center mt-8">
      <a href="#" class="text-emerald-600 hover:text-emerald-700 font-medium">
        Try a different verification method
      </a>
    </p>
  </div>

  <script>
    // Auto-focus next input on typing
    const inputs = document.querySelectorAll('.otp-input');
    inputs.forEach((input, index) => {
      input.addEventListener('input', (e) => {
        const value = e.target.value;
        if (/^\d$/.test(value)) {
          if (index < inputs.length - 1) {
            inputs[index + 1].focus();
          }
        } else {
          e.target.value = '';
        }
      });

      input.addEventListener('keydown', (e) => {
        if (e.key === 'Backspace' && !e.target.value && index > 0) {
          inputs[index - 1].focus();
        }
      });
    });

    // Focus first input on load
    inputs[0].focus();
  </script>
</body>
</html>