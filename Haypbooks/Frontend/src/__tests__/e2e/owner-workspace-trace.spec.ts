import { test } from '@playwright/test'

test('Owner Workspace trace skipped — hub consolidated', async () => {
  console.log('Skipping Owner Workspace trace — Owner Workspace and Accountant Hub have been consolidated into Dashboard')
})