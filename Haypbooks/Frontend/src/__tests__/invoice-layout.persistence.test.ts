// Replaced by .tsx variant of this test to allow JSX parsing under TypeScript.
// Keep a trivial test in this legacy file so jest doesn't treat the suite as empty
describe('legacy placeholder - invoice-layout.persistence.tests', () => {
	it('placeholder', () => { expect(true).toBe(true) })
})
